import unittest
import AccountCreation

class CheckCredentials(AccountCreation):
    def valid_credentials(self):
        self.assertTrue(self.account.authenticate(self.username, self.password))
    def invalid_credentials(self):
        self.assertFalse(self.account.authenticate("wrong_username", "wrong_password"))
